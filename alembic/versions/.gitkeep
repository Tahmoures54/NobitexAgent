# This file keeps the versions/ directory in git even when empty.
# Real migration files are generated by `alembic revision --autogenerate -m "..."`
# and should be committed alongside the code that needs them.